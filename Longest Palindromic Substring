s=input()
maxx=float('-inf')
r=""
for i in range(len(s)):
    for j in range(i+1,len(s)+1):
        if s[i:j]==s[i:j][::-1]:
            if len(s[i:j])>maxx:
                maxx=len(s[i:j])
                r=s[i:j]
print(r)
